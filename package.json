{
  "name": "openai-madeup-endpoint",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
